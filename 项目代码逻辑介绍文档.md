# fmusic 项目代码逻辑介绍文档

## 一、项目概述

### 1.1 项目简介
**fmusic** 是一个基于 SwiftUI 开发的本地音乐播放器，适用于 macOS 系统（最低要求 macOS 11.0）。项目采用 Swift 语言开发，支持多种音频格式，具备歌词解析与卡拉OK效果显示、元数据编辑、收藏管理等完整音乐播放功能。

### 1.2 技术栈
- **开发语言**: Swift
- **UI 框架**: SwiftUI
- **音频播放**: AVFoundation (AVAudioPlayer)
- **元数据处理**: FFmpeg (C API)
- **歌词服务**: LyricsService (第三方库)
- **日志系统**: Logging
- **响应式编程**: Combine
- **开发环境**: Xcode 13.4.1+

### 1.3 支持的音频格式
`.flac`, `.mp3`, `.wav`, `.m4a`, `.aif`, `.m4r`

---

## 二、项目架构

### 2.1 目录结构
```
fmusic/
├── Shared/                          # 共享源码目录
│   ├── musicApp.swift              # 应用入口点
│   ├── AudioPlayer.swift           # 音频播放核心类
│   ├── ContentView.swift           # 主界面视图
│   ├── AboutDelegate.swift         # 关于面板代理
│   ├── ffmpeg.h                    # FFmpeg C 接口声明
│   ├── ffmpeg.c                    # FFmpeg C 实现
│   ├── music (macOS)-Bridging-Header.h  # 桥接头文件
│   ├── Module/                     # 业务模块
│   │   ├── Song.swift             # 歌曲数据模型
│   │   └── LyricsParser/          # 歌词解析模块
│   │       ├── LyricsParser.swift # 歌词解析器
│   │       ├── LyricsItem.swift   # 歌词项模型
│   │       └── LyricsHeader.swift # 歌词头部信息
│   ├── Views/                     # 视图组件
│   │   ├── PlayerView.swift       # 播放器控制视图
│   │   ├── ListContentView.swift  # 歌曲列表视图
│   │   ├── LyricsView.swift       # 歌词显示视图
│   │   ├── FavoritesView.swift    # 收藏列表视图
│   │   ├── MainMenuView.swift     # 主菜单
│   │   ├── MetaDataView.swift     # 元数据编辑视图
│   │   ├── DetailsView.swift      # 文件详情视图
│   │   ├── AboutView.swift        # 关于视图
│   │   ├── ModifiersView.swift    # 视图修饰符
│   │   └── EmpetyListView.swift   # 空列表视图
│   ├── other/                     # 工具类
│   │   └── ArrayUnique.swift      # 数组去重扩展
│   └── Assets.xcassets/           # 资源文件
├── macOS/                         # macOS 平台特定
│   └── macOS.entitlements         # 权限配置
├── Frameworks/                    # 第三方动态库 (FFmpeg)
├── deps/include/                  # FFmpeg 头文件
└── fmusic.xcodeproj/              # Xcode 项目配置
```

### 2.2 架构模式
采用 **MVVM (Model-View-ViewModel)** 架构模式：
- **Model**: `Song`, `LyricsItem`, `LyricsHeader` 等数据模型
- **View**: 所有 SwiftUI 视图组件
- **ViewModel**: `AudioPlayer` 作为核心 ViewModel，管理播放状态、播放列表、歌词等

---

## 三、核心模块详解

### 3.1 应用入口 (musicApp.swift)

**职责**: 应用生命周期管理和全局状态初始化

```swift
@main
struct musicApp: App {
    @NSApplicationDelegateAdaptor(AboutDelegate.self) var aboutDelegate
    @StateObject var player = AudioPlayer()  // 全局唯一的播放器实例
    
    var body: some Scene {
        WindowGroup {
            ContentView(player: player)  // 主内容视图
        }
        .windowStyle(.hiddenTitleBar)  // 隐藏标题栏
        .commands {
            MainMenuView(player: player)  // 自定义菜单
        }
    }
}
```

**关键逻辑**:
1. 使用 `@StateObject` 创建全局 `AudioPlayer` 实例
2. 将播放器实例通过依赖注入传递给所有子视图
3. 配置窗口样式为隐藏标题栏
4. 注册自定义菜单命令

---

### 3.2 音频播放核心 (AudioPlayer.swift)

**职责**: 音频播放控制、播放列表管理、歌词加载与同步

#### 3.2.1 核心属性

```swift
class AudioPlayer: NSObject, ObservableObject, AVAudioPlayerDelegate {
    // 音频播放器
    private var soudPlayer: AVAudioPlayer?
    
    // 发布属性 (UI 绑定)
    @Published var playList: [Song] = []           // 播放列表
    @Published var isPlaying: Bool = false          // 播放状态
    @Published var currentSong = Song()             // 当前歌曲
    @Published var playMode: PlayMode = .Order      // 播放模式
    @Published var volume: Float = 0.7              // 音量
    @Published var albumCover = Image("album")      // 专辑封面
    @Published var currentSongDuration: TimeInterval? = nil  // 歌曲时长
    
    // 歌词相关
    @Published var lyricsParser = LyricsParser()    // 歌词解析器
    @Published var currentLyrics = ""               // 当前歌词
    @Published var offsetTime: Double = 0           // 歌词时间偏移
    @Published var curLyricsIndex = -1              // 当前歌词行索引
    @Published var currentWordIndex: Int? = nil     // 卡拉OK当前字索引
    @Published var currentWordProgress: Double = 0.0 // 卡拉OK字进度
    
    // Combine
    private let currentTimeSubject = PassthroughSubject<TimeInterval, Never>()
    private var cancellables = Set<AnyCancellable>()
}
```

#### 3.2.2 播放模式枚举

```swift
enum PlayMode {
    case Loop    // 列表循环
    case Order   // 顺序播放
    case Random  // 随机播放
    case Single  // 单曲循环
}
```

#### 3.2.3 播放控制流程

**播放下一首 (PlayNext)**:
```
1. 检查播放列表是否为空
2. 根据当前播放模式调用 nextSong() 计算下一首
3. 更新 currentSong
4. 调用 PlayAudio() 加载并播放新歌曲
```

**播放逻辑 (nextSong 函数)**:
- **Loop**: `(currentIndex + 1) % playList.count` - 取模实现循环
- **Order**: `currentIndex + 1` - 到达末尾返回当前歌曲
- **Random**: 随机索引，避免连续播放同一首
- **Single**: 始终返回当前歌曲

**上一首 (PlayPrev)**: 逻辑类似，方向相反

#### 3.2.4 音频文件加载流程

```swift
func PlayAudio(path: String) {
    // 1. 验证文件路径
    guard !path.isEmpty, fileExists(atPath: path) else { return }
    
    // 2. 停止并清理之前的播放器
    stopAndCleanup()
    
    // 3. 重置歌词状态
    reset()
    
    // 4. 创建 AVAudioPlayer 实例
    soudPlayer = try AVAudioPlayer(contentsOf: url)
    soudPlayer?.delegate = self
    soudPlayer?.volume = volume
    
    // 5. 准备播放并获取时长
    soudPlayer?.prepareToPlay()
    currentSongDuration = soudPlayer?.duration
    
    // 6. 开始播放
    soudPlayer?.play()
    isPlaying = true
    startUpdateTimer()
    
    // 7. 加载歌词 (本地文件或网络下载)
    loadLyrics()
}
```

#### 3.2.5 定时器与时间更新

```swift
// 启动定时器 (每 0.05 秒触发)
private func startUpdateTimer() {
    updateTimer = Timer.scheduledTimer(
        timeInterval: 0.05,
        target: self,
        selector: #selector(timerFired),
        userInfo: nil,
        repeats: true
    )
    RunLoop.current.add(updateTimer!, forMode: .common)
}

// 定时器触发
@objc private func timerFired() {
    let currentTime = soudPlayer?.currentTime ?? 0
    currentTimeSubject.send(currentTime)  // 发布当前时间
    updateKaraokeProgress(currentTime: currentTime)  // 更新卡拉OK进度
}
```

#### 3.2.6 卡拉OK进度计算

```swift
private func updateKaraokeProgress(currentTime: TimeInterval) {
    // 1. 获取当前歌词行
    let currentLine = lyricsParser.lyrics[curLyricsIndex]
    let wordInfos = currentLine.wordInfos
    
    // 2. 计算相对于当前行开始的时间
    let timeWithinLine = currentTime - currentLine.time + offsetTime
    
    // 3. 查找当前时间应该高亮到哪个字
    if let foundIndex = wordInfos.lastIndex(where: { $0.startTime <= timeWithinLine }) {
        currentWordIndex = foundIndex
        // 计算该字已进行的进度 (0.0 到 1.0)
        let timeInWord = timeWithinLine - currentWordInfo.startTime
        currentWordProgress = timeInWord / currentWordInfo.duration
    }
}
```

#### 3.2.7 歌词加载流程

```
1. 检查本地歌词文件是否存在 (~/Music/Lyrics/歌曲名 - 艺术家.lrcx)
2. 如果存在 → loadLyricsFromFile()
3. 如果不存在 → searchLyrics() 网络搜索
   - 使用 LyricsService 库
   - 搜索源: 酷狗、QQ、网易云、 gecimi
   - 超时时间: 5 秒
4. 歌词过滤策略 (4 次尝试):
   - FirstTime: 精确匹配歌名 + 歌手 + 时长
   - SecondTime: 模糊匹配歌名 + 歌手 + 时长
   - ThirdTime: 模糊匹配歌名 + 时长
   - LastTime: 只匹配时长
5. 下载成功后写入本地文件并加载
```

---

### 3.3 数据模型 (Song.swift)

```swift
struct Song: Identifiable, Hashable {
    let id = UUID()                    // 唯一标识符
    
    var name: String                   // 歌曲名
    var artist: String                 // 艺术家
    var album: String                  // 专辑名
    var duration: TimeInterval         // 时长 (秒)
    var filePath: String               // 文件路径
    var isSelected: Bool               // 是否选中
    var isPlaying: Bool                // 是否正在播放
    var isHeartChecked: Bool           // 是否收藏
    var track: Int                     // 音轨序号
}
```

**设计要点**:
- 遵循 `Identifiable` 协议，用于 SwiftUI 列表渲染
- 遵循 `Hashable` 协议，支持集合操作
- 使用 `UUID` 作为唯一标识，避免文件路径冲突

---

### 3.4 歌词解析模块 (LyricsParser)

#### 3.4.1 歌词数据模型

```swift
// 歌词头部信息
struct LyricsHeader {
    var title: String?      // ti: 标题
    var author: String?     // ar: 作者
    var album: String?      // al: 专辑
    var by: String?         // by: 歌词制作
    var offset: TimeInterval = 0  // offset: 时间偏移 (毫秒)
    var editor: String?     // re: 编辑器
    var version: String?    // ve: 版本
    var longSec: TimeInterval = 0  // length: 总时长
}

// 歌词项 (每一行)
class LyricsItem: Identifiable, Hashable {
    var id: UUID
    var time: TimeInterval           // 时间戳 (秒)
    var text: String                 // 歌词文本
    var wordInfos: [WordInfo] = []   // 逐字信息 (卡拉OK)
}

// 逐字信息 (卡拉OK效果)
struct WordInfo: Identifiable, Hashable {
    var id = UUID()
    var word: String                 // 单个字/词
    var startTime: TimeInterval      // 相对于该行的开始时间 (秒)
    var duration: TimeInterval       // 持续时间 (秒)
}
```

#### 3.4.2 歌词解析流程

```
1. 按行分割歌词文本
2. 第一次遍历:
   - 解析头部信息 (parseHeaderLine)
   - 解析时间标签 [mm:ss.xx]
   - 区分普通歌词行和 [tt] 卡拉OK行
   - 按时间戳存储到字典 lineInfosByTime
3. 第二次遍历:
   - 按时间排序
   - 合并普通歌词文本和 [tt] 内容
   - 解析 [tt] 标签生成 WordInfo 数组
   - 生成最终的 LyricsItem 列表
```

**[tt] 标签解析示例**:
```
普通歌词行: [00:12.34]我爱你中国
卡拉OK行:   [00:12.34][tt]<0,0><337,1><674,2>...

解析结果:
WordInfo[0]: word="我", startTime=0.0, duration=0.337
WordInfo[1]: word="爱", startTime=0.337, duration=0.337
WordInfo[2]: word="你", startTime=0.674, duration=...
```

---

### 3.5 FFmpeg 集成 (ffmpeg.c / ffmpeg.h)

#### 3.5.1 功能概述

FFmpeg 用于处理苹果原生 API 不支持的音频元数据读取和编辑：

1. **读取音频文件元数据** (GetMetadata)
2. **提取内嵌专辑封面** (GetAlbumCoverImage)
3. **修改音频文件元数据** (UpdateSongMeta)

#### 3.5.2 核心 C 函数

```c
// 打开音频文件，返回 AVFormatContext
AVFormatContext* open_audio_file_fmt_ctx(const char *filename);

// 提取内嵌专辑封面，返回 AVPacket
AVPacket* get_album_cover_image(const char *filename);

// 修改元数据并生成新文件
int modify_meta(const char* in_filename, 
                const char* out_filename, 
                AVDictionary *new_metadata);

// 替换文件
int replace_file(const char *dst, const char *src);
```

#### 3.5.3 元数据读取流程 (GetMetadata)

```swift
func GetMetadata(path: String) -> Song? {
    // 1. 调用 FFmpeg 打开文件
    var fmt_ctx = open_audio_file_fmt_ctx(filename)
    
    // 2. 获取时长
    s.duration = fmt_ctx.pointee.duration / AV_TIME_BASE
    
    // 3. 遍历元数据字典
    while let tag = av_dict_get(metadata, "", prev, AV_DICT_IGNORE_SUFFIX) {
        let key = String(cString: tag.pointee.key)
        let value = String(cString: tag.pointee.value)
        switch key {
        case "title": s.name = value
        case "album": s.album = value
        case "artist": s.artist = value
        case "track": s.track = value.integerValue
        }
    }
    
    // 4. 如果没有歌曲名，使用文件名
    if s.name.isEmpty {
        s.name = URL(fileURLWithPath: path).lastPathComponent
    }
    
    // 5. 释放资源
    avformat_close_input(&fmt_ctx)
    return s
}
```

#### 3.5.4 专辑封面提取流程

```swift
func GetAlbumCoverImage(path: String) -> Image? {
    // 1. 调用 FFmpeg 获取专辑封面数据包
    guard let pkt = get_album_cover_image(path) else { return nil }
    
    // 2. 从 AVPacket 提取图片数据
    let data = pkt.pointee.data
    let size = pkt.pointee.size
    let nsData = NSData(bytes: data, length: Int(size))
    
    // 3. 创建 NSImage
    if let nsImage = NSImage(data: nsData as Data) {
        return Image(nsImage: nsImage)
    }
    
    // 4. 释放资源
    av_packet_unref(pkt)
    av_free(pkt)
    return nil
}
```

#### 3.5.5 元数据修改流程

```swift
func UpdateSongMeta(song: Song) -> Bool {
    // 1. 创建临时文件路径
    let tmpFile = ".../new" + filename
    
    // 2. 设置新元数据
    av_dict_set(&newMetaData, "track", String(song.track), 0)
    av_dict_set(&newMetaData, "title", song.name, 0)
    av_dict_set(&newMetaData, "album", song.album, 0)
    av_dict_set(&newMetaData, "artist", song.artist, 0)
    
    // 3. 调用 FFmpeg 修改元数据 (生成新文件)
    if modify_meta(song.filePath, tmpFile, newMetaData) != 0 {
        return false
    }
    
    // 4. 替换原文件
    if replace_file(song.filePath, tmpFile) != 0 {
        return false
    }
    
    return true
}
```

---

## 四、UI 视图模块

### 4.1 主界面布局 (ContentView.swift)

```
┌─────────────────────────────────────────────────────┐
│  HSplitView                                         │
│  ┌──────────────────────┬──────────────────────────┐ │
│  │  ZStack              │  LyricsView (可选)       │ │
│  │  ┌──────────────────┐│                          │ │
│  │  │ ListContentView  ││  歌词显示区域            │ │
│  │  │ (歌曲列表)       ││  - 歌曲标题               │ │
│  │  └──────────────────┘│  - 滚动歌词               │ │
│  │  ┌──────────────────┐│  - 时间偏移滑块           │ │
│  │  │ PlayerView       ││                          │ │
│  │  │ (播放控制栏)     ││                          │ │
│  │  └──────────────────┘│                          │ │
│  └──────────────────────┴──────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 4.2 播放器控制视图 (PlayerView.swift)

**组成元素**:
1. **专辑封面**: 圆形显示，鼠标悬停放大
2. **歌曲信息**: 艺术家、歌曲名、当前歌词
3. **进度条**: 可拖拽调整播放位置
4. **控制按钮**:
   - 收藏按钮 (HeartButton)
   - 播放模式按钮 (PlayModeButton)
   - 播放/暂停/上一首/下一首
5. **音量滑块**: 范围 0-1.0

**进度条实现 (ProgressBar)**:
```swift
struct ProgressBar: View {
    @ObservedObject var player: AudioPlayer
    @State var isEditing: Bool = false  // 是否正在拖拽
    
    var body: some View {
        ProgressView(value: percentage, total: 1.0)
            .gesture(DragGesture()
                .onChanged { value in
                    // 实时更新进度和预估时间
                    progressWidth = currentWidth
                    percentage = currentWidth / progressMaxWidth
                }
                .onEnded { value in
                    // 跳转到目标时间
                    player.SetCurrentTime(value: targetTime)
                }
            )
            .onReceive(player.currentTimePublisher) { currentTime in
                // 接收播放器时间更新
                percentage = currentTime / duration
            }
    }
}
```

**播放模式切换**:
```
Loop (列表循环) → Order (顺序) → Random (随机) → Single (单曲) → Loop
图标: repeat.circle → list.bullet.circle → shuffle.circle → repeat.1.circle
```

### 4.3 歌曲列表视图 (ListContentView.swift)

**功能模块**:
1. **搜索框**: 模糊匹配歌曲名/艺术家/专辑
2. **侧边导航**:
   - 资料库 (LibraryView)
   - 我的收藏 (FavoritesView)
3. **工具栏**:
   - 导入音乐文件夹 (folder.badge.plus)
   - 清空资料库 (trash)
   - 切换歌词显示 (text.bubble)

**LibraryView**:
- 显示表头: 歌曲名、艺术家、专辑、时长
- 搜索过滤: `playList.filter { name.contains || album.contains || artist.contains }`
- 行交互:
  - 双击: 播放该歌曲
  - 单击: 选中行 (改变背景色)
  - 右键菜单: 编辑元信息、文件详情

### 4.4 歌词视图 (LyricsView.swift)

**核心功能**:
1. **歌词滚动**: ScrollViewReader 自动滚动到当前歌词行
2. **卡拉OK效果**: KaraokeLineView 逐字高亮显示
3. **时间偏移调整**: Slider 范围 -30s ~ +30s

**卡拉OK行渲染 (KaraokeLineView)**:
```swift
struct KaraokeLineView: View {
    var body: some View {
        if line.wordInfos.isEmpty {
            // 普通歌词模式
            Text(line.text)
                .foregroundColor(isCurrentLine ? .white : .gray)
        } else {
            // 卡拉OK模式 - 逐字渲染
            HStack(spacing: 0) {
                ForEach(line.wordInfos) { wordInfo in
                    renderWord(wordInfo: wordInfo)
                }
            }
        }
    }
    
    // 使用 ZStack + Mask 实现颜色渐变
    private func renderWord(wordInfo: WordInfo) -> some View {
        ZStack(alignment: .leading) {
            Text(wordInfo.word).foregroundColor(.gray)  // 底层
            Text(wordInfo.word).foregroundColor(.white) // 上层
                .mask(Rectangle().frame(width: progress)) // 裁剪
        }
    }
}
```

### 4.5 收藏视图 (FavoritesView.swift)

**逻辑**:
- 过滤 `playList` 中 `isHeartChecked == true` 的歌曲
- 显示逻辑与 LibraryView 相同
- 支持搜索过滤

### 4.6 元数据编辑视图 (MetaDataView.swift)

**可编辑字段**:
- 音轨序号 (track)
- 歌曲名
- 专辑
- 艺术家

**保存流程**:
1. 调用 `UpdateSongMeta(song)` 修改文件元数据
2. 调用 `player.ChangeMetaDataOneOfList(changeOne: song)` 更新列表显示

### 4.7 文件详情视图 (DetailsView.swift)

**显示信息**:
- Track、歌曲名、专辑、艺术家、时长
- 文件完整路径

**重命名功能**:
1. 提供"获取标准文件名"按钮 (格式: Track-歌曲名-艺术家)
2. 输入新文件名
3. 调用 `rename()` 系统函数重命名文件
4. 更新播放列表中的文件路径

---

## 五、辅助工具模块

### 5.1 菜单系统 (MainMenuView.swift)

**快捷键**:
- `⌘ + O`: 导入音乐文件夹
- `⌘ + D`: 清空资料库

### 5.2 文件夹导入 (OpenSelectFolderWindws)

**流程**:
```
1. 打开 NSOpenPanel (仅允许选择文件夹)
2. 用户选择文件夹后，在后台线程处理:
   a. 遍历选中文件夹
   b. 递归扫描子目录
   c. 过滤支持的音频格式
   d. 提取元数据 (GetMetadata)
   e. 按 专辑 > 艺术家 > 音轨序号 排序
3. 回到主线程追加到 playList
```

### 5.3 视图修饰符 (ModifiersView.swift)

提供可复用的 UI 效果:

| 修饰符 | 效果 |
|--------|------|
| `pinkBackgroundOnHover` | 鼠标悬停粉色背景，前景色变白 |
| `itemBackgroundOnHover` | 列表项悬停黄色背景+缩放 |
| `imageOnHover` | 图片悬停放大+偏移 |
| `circleImage` | 圆形裁剪+白色描边+阴影 |

### 5.4 数组去重扩展 (ArrayUnique.swift)

```swift
extension Sequence where Iterator.Element: Hashable {
    func unique() -> [Iterator.Element] {
        var seen: Set<Iterator.Element> = []
        return filter { seen.insert($0).inserted }
    }
}
```

### 5.5 时间格式化

```swift
func durationFormat(timeInterval: TimeInterval) -> String {
    let time = max(0, timeInterval)
    if !time.isFinite { return "00:00" }
    let interval = Int(time)
    let seconds = interval % 60
    let minutes = (interval / 60) % 60
    return String(format: "%02d:%02d", minutes, seconds)
}
```

---

## 六、数据流分析

### 6.1 播放流程

```
用户双击歌曲
    ↓
设置 player.currentSong
    ↓
PlayerView.onChange(of: currentSong.filePath) 触发
    ↓
1. 获取专辑封面
2. 停止当前播放
3. 调用 player.PlayAudio(path)
    ↓
PlayAudio 内部流程:
    ├─ 停止并清理旧播放器
    ├─ 重置歌词状态
    ├─ 创建 AVAudioPlayer
    ├─ 准备播放并获取时长
    ├─ 开始播放
    ├─ 启动定时器
    └─ 加载歌词 (本地或下载)
    ↓
定时器每 0.05s 更新:
    ├─ 发送 currentTime 到 Publisher
    ├─ 进度条更新
    └─ 卡拉OK进度更新
```

### 6.2 歌词同步流程

```
音频播放中
    ↓
timerFired() 触发
    ↓
currentTimeSubject.send(currentTime)
    ↓
ProgressBar.onReceive 更新进度条
    ↓
updateLyrics(currentTime) 计算当前歌词行:
    ├─ effectiveTime = currentTime + offsetTime
    ├─ 查找 lastIndex where { $0.time <= effectiveTime }
    └─ 更新 player.curLyricsIndex 和 player.currentLyrics
    ↓
LyricsView.onChange(of: curId) 触发滚动
    ↓
KaraokeLineView 更新逐字高亮:
    ├─ currentWordIndex
    └─ currentWordProgress
```

### 6.3 Combine 响应式数据流

```
AudioPlayer.currentTimeSubject (PassthroughSubject)
    ↓
ProgressBar.onReceive
    ↓
更新 percentage 和 currentTimeText
    ↓
UI 自动刷新

AudioPlayer.$currentSongDuration (Published)
    ↓
ProgressBar.onReceive
    ↓
更新 totalTimeText
```

---

## 七、关键技术点

### 7.1 异步歌词搜索

```swift
searchLyrics(song:artist:timeout:completion:) -> AnyCancellable?
```

**要点**:
- 使用 `LyricsService` 库的多源搜索
- 设置 5 秒超时限制
- 异步回调中检查歌曲是否已切换 (防止歌词张冠李戴)
- 使用 `cancellables` 管理订阅生命周期

### 7.2 卡拉OK逐字高亮

**实现原理**:
1. 解析 LRCX 格式的 `[tt]` 标签
2. 提取 `<startTime, wordIndex>` 对
3. 使用 ZStack + Mask 实现颜色裁剪效果
4. 定时器高频更新 `currentWordIndex` 和 `currentWordProgress`

### 7.3 FFmpeg 与 Swift 桥接

**桥接头文件** (`music (macOS)-Bridging-Header.h`):
```objc
#include "ffmpeg.h"
```

**Swift 调用 C 函数**:
```swift
var fmt_ctx = open_audio_file_fmt_ctx(filename)
av_dict_get(metadata, "", prev, AV_DICT_IGNORE_SUFFIX)
```

**C 回调 Swift**: 通过闭包/代理模式

### 7.4 后台线程处理

**导入文件夹时**:
```swift
DispatchQueue.global(qos: .userInteractive).async {
    // 后台: 扫描文件、提取元数据、排序
    let sortedSongs = loadedSongs.sorted { ... }
    
    DispatchQueue.main.async {
        // 主线程: 更新 UI
        player.playList.append(contentsOf: sortedSongs)
    }
}
```

### 7.5 元数据修改的原子性

**修改流程**:
```
原文件 → FFmpeg 处理 → 临时文件 → 删除原文件 → 重命名临时文件
```

**优势**:
- 避免原文件损坏风险
- 保证操作的原子性

---

## 八、扩展与优化方向

### 8.1 已完成功能
- ✅ 多格式音频播放
- ✅ 自动解析专辑信息和封面
- ✅ 四种播放模式
- ✅ 歌词自动下载与卡拉OK效果
- ✅ 元数据编辑
- ✅ 收藏管理
- ✅ 搜索过滤
- ✅ 文件重命名

### 8.2 待开发功能
- ⬜ 播放列表管理 (PlayList)
- ⬜ macOS 10.15 向下兼容
- ⬜ 更多音频格式支持
- ⬜ 均衡器/音效
- ⬜ 迷你播放器模式

### 8.3 技术优化建议
1. **性能**: 大播放列表的虚拟滚动 (LazyVStack)
2. **架构**: 将 FFmpeg 调用封装为独立 Service 层
3. **测试**: 添加单元测试 (歌词解析、元数据提取)
4. **错误处理**: 更友好的错误提示与恢复机制
5. **内存管理**: 及时释放不用的 AVAudioPlayer 实例

---

## 九、常见问题 (FAQ)

### 9.1 "文件损坏"或"无法打开"错误
**原因**: macOS Gatekeeper 限制
**解决**: 
```bash
sudo xattr -d com.apple.quarantine /Applications/fmusic.app
```

### 9.2 歌词不匹配
**原因**: 
- 歌词文件命名不匹配 (格式: `歌曲名 - 艺术家.lrcx`)
- 多个版本歌词冲突
**解决**: 手动调整歌词时间偏移或使用标准文件名

### 9.3 元数据修改失败
**原因**: 
- 文件格式不支持 (如某些 WAV)
- 文件权限问题
**解决**: 检查文件写权限，确认格式兼容性 (FLAC/MP3/M4A 最佳)

---

## 十、总结

**fmusic** 是一个功能完整的 macOS 本地音乐播放器，核心特点:

1. **纯 SwiftUI 实现**: 声明式 UI，响应式数据绑定
2. **FFmpeg 深度集成**: 处理苹果原生不支持的音频格式和元数据
3. **卡拉OK歌词**: 支持 LRCX 格式的逐字高亮效果
4. **响应式架构**: Combine + @Published 实现数据驱动 UI
5. **后台处理**: 耗时操作 (文件扫描、歌词下载) 均在后台线程

项目代码结构清晰，模块职责明确，具备良好的可扩展性和维护性。

---

**文档生成日期**: 2026年4月15日  
**项目版本**: v1.1.0  
**代码语言**: Swift 5.x  
**最低系统要求**: macOS 11.0
