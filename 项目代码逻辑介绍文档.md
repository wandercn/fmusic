# fmusic 项目代码逻辑介绍文档

## 一、项目概述

### 1.1 项目简介
**fmusic** 是一个基于 SwiftUI 开发的本地音乐播放器，适用于 macOS 系统（最低要求 macOS 11.0）。项目采用 Swift 语言开发，支持多种音频格式，具备歌词解析与卡拉OK效果显示、元数据编辑、收藏管理等完整音乐播放功能。

### 1.2 技术栈
- **开发语言**: Swift
- **UI 框架**: SwiftUI
- **音频播放**: AVFoundation (AVAudioPlayer)
- **元数据处理**: FFmpeg (C API)
- **歌词服务**: LyricsService (第三方库)
- **日志系统**: Logging
- **响应式编程**: Combine
- **开发环境**: Xcode 13.4.1+

### 1.3 支持的音频格式
`.flac`, `.mp3`, `.wav`, `.m4a`, `.aif`, `.m4r`

---

## 二、项目架构

### 2.1 目录结构
```
fmusic/
├── Shared/                          # 共享源码目录
│   ├── musicApp.swift              # 应用入口点
│   ├── AudioPlayer.swift           # 音频播放核心类
│   ├── ContentView.swift           # 主界面视图
│   ├── AboutDelegate.swift         # 关于面板代理
│   ├── ffmpeg.h                    # FFmpeg C 接口声明
│   ├── ffmpeg.c                    # FFmpeg C 实现
│   ├── music (macOS)-Bridging-Header.h  # 桥接头文件
│   ├── Module/                     # 业务模块
│   │   ├── Song.swift             # 歌曲数据模型
│   │   ├── Playlist.swift         # 播放列表模型 (New)
│   │   └── LyricsParser/          # 歌词解析模块
│   ├── Views/                     # 视图组件
│   │   ├── PlayerView.swift       # 播放器控制视图
│   │   ├── ListContentView.swift  # 歌曲列表容器 (重构)
│   │   ├── PlaylistsView.swift    # 播放列表视图 (New)
│   │   ├── LyricsView.swift       # 歌词显示视图
│   │   ├── FavoritesView.swift    # 收藏列表视图
│   │   ├── MainMenuView.swift     # 主菜单
│   │   ├── MetaDataView.swift     # 元数据编辑视图
│   │   ├── DetailsView.swift      # 文件详情视图
│   │   └── ...
├── Frameworks/                    # 第三方动态库 (FFmpeg)
└── fmusic.xcodeproj/              # Xcode 项目配置
```

### 2.2 架构模式 (重构后)
采用 **MVVM** 模式，重点优化了视图与数据的解耦：
- **ViewModel (AudioPlayer)**: 引入了 `librarySongs` (全量曲库) 与 `playbackQueue` (当前播放队列) 的分离。
- **View (NavigationView)**: 采用标准的侧边栏导航结构，左侧管理目录与列表，右侧动态渲染歌曲详情。

---

## 三、核心模块详解

### 3.1 播放器控制核心 (AudioPlayer.swift)

**重构亮点**: **曲库与播放队列分离**。

#### 3.2.1 核心属性
```swift
class AudioPlayer: NSObject, ObservableObject {
    @Published var librarySongs: [Song] = []    // 全量曲库
    @Published var playlists: [Playlist] = []   // 用户定义的播放列表
    @Published var playbackQueue: [Song] = []   // 当前正在播放的队列
    @Published var currentSong = Song()         // 当前正在播放的单曲
    
    @Published var currentPlaylistIndex: Int = -1 // 视图选中的索引 (-1 为曲库)
}
```

#### 3.2.2 播放逻辑
当用户在任一列表中双击歌曲时，触发 `play(song:in queue:)`。此操作会将该列表设为 `playbackQueue`，而用户随后切换侧边栏查看其他列表时，`playbackQueue` 保持不变，确保切歌逻辑（下一首/随机）始终在正确的上下文中运行。

---

### 3.2 播放列表管理 (Playlist.swift)

**数据结构**:
```swift
struct Playlist: Identifiable, Codable {
    let id: UUID
    var name: String
    var songs: [Song]
}
```

**管理功能**:
- **持久化**: 自动同步至 `UserDefaults`。
- **交互**: 支持从曲库右键菜单“添加到播放列表”。
- **兼容性**: 针对 macOS 11 系统，创建与重命名采用自定义 Sheet 弹窗而非 macOS 12+ 的 Alert TextField。

---

### 3.3 UI 结构与切换逻辑 (ListContentView.swift)

**侧边栏结构**:
1. **搜索栏**: 全局实时过滤。
2. **资料库**: 包含“所有歌曲”入口。
3. **播放列表**: 展示用户创建的列表，支持右键重命名/删除。
4. **已保存目录**: 展示本地扫描的物理路径。

**内容切换**:
通过 `selectedPlaylist` 状态变量，在 `NavigationView` 的 Detail 区域使用 `@ViewBuilder` 条件渲染 `LibraryView` 或 `PlaylistsView`。这种方式避免了视图叠加，保证了 UI 的响应速度。

---

## 四、UI 优化 (v1.1.1)

### 4.1 表头修复
解决了 SwiftUI `List` 在 `NavigationView` 中可能出现的表头遮挡问题。
- 移除了负边距 hack。
- 使用 `NSColor.windowBackgroundColor` 确保深浅色模式下的视觉一致性。
- 增加底部阴影分割线。

### 4.2 播放图标实时同步
不再依赖 `Song` 模型内的 `isPlaying` 脏状态，而是统一在视图层使用：
```swift
if player.currentSong.id == song.id {
    // 显示红色播放图标
}
```
这保证了当同一首歌出现在多个列表（如曲库、精选列表、最近播放）时，播放状态能跨视图实时同步。

---

## 五、开发与兼容性说明

### 5.1 macOS 11.0 适配
本项目严格遵循 macOS 11.0 SDK 规范：
- 替换了所有 macOS 12+ 专有的 API（如 `.overlay { }` 闭包，`.background(.red)` 简化语法等）。
- 使用 `sheet` 替代了 macOS 12 的交互式 `alert`。
- 解决了复杂视图在旧版编译器下的类型检查超时问题。

---

**文档更新日期**: 2026年4月16日  
**项目版本**: v1.1.1  
**修复重点**: 播放列表、架构解耦、UI 同步、macOS 11 兼容性
